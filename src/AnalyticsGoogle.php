<?php

/**
 * Main AnalyticsGoogle class.
 *
 * Entry point for the Google Analytics (GA4) provider that plugs into
 * the ArtisanPack UI analytics package. Uses the shared google base
 * package for OAuth2 and token management. Accessed via the
 * `analyticsGoogle()` helper function or the AnalyticsGoogle facade.
 *
 * @package    ArtisanPack_UI
 * @subpackage AnalyticsGoogle
 *
 * @since      1.0.0
 */

declare( strict_types=1 );

namespace ArtisanPackUI\AnalyticsGoogle;

/**
 * Main AnalyticsGoogle class.
 *
 * Add GA4 Data API reporting and provider registration methods here
 * as the package grows.
 *
 * @package    ArtisanPack_UI
 * @subpackage AnalyticsGoogle
 *
 * @since      1.0.0
 */
class AnalyticsGoogle
{
    // Add your package methods here
}
